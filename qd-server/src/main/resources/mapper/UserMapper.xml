<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.qd.mapper.UserMapper">

    <insert id="createUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password_hash, email, mobile, nickname, avatar_url, role, role_level, status,
                          last_login_at, last_login_ip, extra, remark, created_at, updated_at, deleted_at)
        VALUES (#{username}, #{passwordHash}, #{email}, #{mobile}, #{nickname}, #{avatarUrl}, #{role}, #{roleLevel}, #{status},
                #{lastLoginAt}, #{lastLoginIp}, #{extra}, #{remark}, #{createdAt}, #{updatedAt}, #{deletedAt})
    </insert>


    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from user
        <where>
            <if test="begin != null">
                and create_time &gt; #{begin}
            </if>
            <if test="end != null">
                and create_time &lt; #{end}
            </if>
        </where>
    </select>

</mapper>
